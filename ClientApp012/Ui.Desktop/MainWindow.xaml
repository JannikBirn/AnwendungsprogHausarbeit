<Window x:Class="De.HsFlensburg.ClientApp012.Ui.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:De.HsFlensburg.ClientApp012.Ui.Desktop"
        xmlns:viewModel="clr-namespace:De.HsFlensburg.ClientApp012.Logic.Ui.Wrapper;assembly=Logic.Ui"
        xmlns:viewModels="clr-namespace:De.HsFlensburg.ClientApp012.Logic.Ui.ViewModels;assembly=Logic.Ui"
        mc:Ignorable="d"
        Title="MainWindow" Height="211.2" Width="500">
    <Window.Resources>
        
    </Window.Resources>
    <Window.DataContext>
        <Binding Path="MainWindowVM" Source="{StaticResource ViewModelLocator}"/>
    </Window.DataContext>
    <Window.IsEnabled>
        <Binding Source="{StaticResource MessageListener}" 
                 Path="BindableProperty"></Binding>
    </Window.IsEnabled>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <UniformGrid Grid.Column="0" Columns ="1" Background="#FF252526">
            <Button Command="{Binding OpenNewClientWindow, Mode=OneWay}" Background="#FF333337" BorderBrush="#FF404045" Foreground="White">Neuer Kunde</Button>
        </UniformGrid>
        <DataGrid Grid.Column="1" ItemsSource="{Binding Path=MyList}"></DataGrid>
    </Grid>
</Window>
